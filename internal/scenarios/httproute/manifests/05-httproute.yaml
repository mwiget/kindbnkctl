apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: scn-route
  namespace: scn-httproute
spec:
  parentRefs:
  - name: scn-gateway
  hostnames:
  - kindbnkctl.local
  rules:
  - matches:
    - path: {type: PathPrefix, value: /}
    backendRefs:
    - name: nginx
      port: 80
